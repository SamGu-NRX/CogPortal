# Deprecated

This synthetic v1 fixture is retained temporarily for history only. Active Week
2 execution uses the pinned `benchmarks/week2` submodule and the
`cogworks.benchmarks.v2` plugins. Do not install or extend this package.
