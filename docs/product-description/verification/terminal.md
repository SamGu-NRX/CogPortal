# Verification: the terminal

How to run this file: work from a checkout of a test team repository that you can push to, with `.venv-test/bin/python` on `PATH` or the CLI installed into that virtualenv. Point every command at the dev portal with `--portal http://localhost:PORT`, which is allowed because the host is loopback. Start from an unlinked device: `rm ~/.cogbench/config.json` (or set `COGBENCH_CONFIG` to a throwaway path, which is safer and is what these items assume). Clear `.cogbench/reports/` between sections where an item says so. Most items here are scriptable; the ones marked `tty` in the Needs column render only to a terminal and must be watched.

`terminal/sync.md` describes work that was in flight during drafting. Re-read `python/cogbench/src/cogbench/cli.py`, `client.py`, and `apps/portal/worker/routes/local-reports.ts` before running `SYNC-*`.

## terminal/status.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| STATUS-01 | P1 | device | Prints exactly seven lines in a fixed order with labels padded to a fixed width ([The simple case](../terminal/status.md#the-simple-case)). | A linked device whose account is on a team. | 1. `cogworks status`<br>2. Count the lines. | Seven lines, in order GitHub, Team, Repo, Discord, Device, Expires, Portal. Exit 0. | not run |
| STATUS-02 | P1 | none | With no portal selected it refuses before any request ([Asking](../terminal/status.md#asking)). | `COGBENCH_CONFIG` pointing at a path that does not exist, no `COGPORTAL_URL`. | 1. `cogworks status`<br>2. `echo $?` | stderr: `cogworks: No CogPortal is selected. Copy \`cogworks link --portal ...\` from the setup page.` Nothing on stdout. Exit 2. | not run |
| STATUS-03 | P1 | none | A non-HTTPS non-loopback origin is rejected ([Asking](../terminal/status.md#asking)). | Any config. | 1. `cogworks status --portal http://example.com` | stderr: `cogworks: CogPortal must use HTTPS (HTTP is allowed only for local development).` Exit 2. | not run |
| STATUS-04 | P1 | none | An origin carrying a path or credentials is rejected ([Asking](../terminal/status.md#asking)). | Any config. | 1. `cogworks status --portal https://user:pw@example.com/x` | Same HTTPS message, exit 2. Record the exact text; the document claims one message covers both faults. | not run |
| STATUS-05 | P1 | device | An expired saved token reads as "not linked" ([Asking](../terminal/status.md#asking)). | Edit the config's `expiresAt` for this origin to a past millisecond value. | 1. `cogworks status` | stderr: `cogworks: This portal is not linked. Run \`cogworks link\` first.` Exit 2. | not run |
| STATUS-06 | P2 | device | The Discord line shows a raw channel snowflake, not a name (suspected bug) ([The simple case](../terminal/status.md#the-simple-case)). | A team with a bound Discord channel. | 1. `cogworks status`<br>2. Read the Discord line. | Record what happens. The document expects `Discord  #` followed by a numeric id. | not run |
| STATUS-07 | P2 | device | With no channel bound the Discord line is words, not an id ([The simple case](../terminal/status.md#the-simple-case)). | A team with no bound channel. | 1. `cogworks status` | `Discord  team channel not chosen` | not run |
| STATUS-08 | P3 | device | The expiry is ISO 8601 UTC ending in `Z` ([The simple case](../terminal/status.md#the-simple-case)). | Linked device. | 1. `cogworks status`<br>2. Read the Expires line. | A timestamp of the form `2026-10-01T14:22:05Z`. Not a locale format. | not run |
| STATUS-09 | P2 | device, piped | Output is byte-identical piped and on a terminal ([Modifiers](../terminal/status.md#modifiers)). | Linked device. | 1. `cogworks status > a.txt`<br>2. `cogworks status \| cat > b.txt`<br>3. `diff a.txt b.txt` | No difference. No colour codes in either. | not run |
| STATUS-10 | P2 | device | `--portal` applies to one invocation and does not change the active portal ([Modifiers](../terminal/status.md#modifiers)). | Two linked portals, one active. | 1. `cogworks status --portal <the other>`<br>2. `cogworks status` | The first reports the other portal, the second reports the original active one. | not run |
| STATUS-11 | P1 | device | Nothing is written, including no clearing of a token judged expired ([Answered without work](../terminal/status.md#answered-without-work)). | Config with an expired token. | 1. Record the config file's bytes and mtime.<br>2. `cogworks status`<br>3. Compare. | The file is byte-identical and its mtime is unchanged. | not run |
| STATUS-12 | P1 | device | A response missing a field produces a traceback, not a sentence (suspected bug) ([Edge cases](../terminal/status.md#edge-cases)). | A stub portal that answers `/api/v1/cli/device/status` with `{}` and HTTP 200. | 1. `cogworks status --portal <stub>` | Record what happens. The document expects an unhandled `KeyError` traceback. | not run |
| STATUS-13 | P3 | device, offline | The retry window is silent (suspected: long enough to look hung) ([While it works](../terminal/status.md#while-it-works)). | Linked device, then pull the network. | 1. `cogworks status`<br>2. Time from the return key to the first output. | Record the elapsed seconds and whether anything was printed meanwhile. The document expects silence for up to roughly 45 s. | not run |
| STATUS-14 | P2 | device | An account with no team gets the portal's own sentence ([How it ends](../terminal/status.md#how-it-ends)). | A device linked to an account that is not on a team, if that state is reachable. | 1. `cogworks status` | `cogworks: Finish joining a team and connecting its repository first.` Exit 2. If the state cannot be produced, mark blocked and say so. | not run |

Not checkable by hand:

- Whether anything scripts against this output, which would decide whether the missing `--json` mode matters. That is a product question.
- Whether an account can hold two team memberships. The schema forbids it; producing the state would mean editing the database directly.

## terminal/check.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CHECK-01 | P1 | none | Exit 0 requires all five signals ([the exit code section](../terminal/check.md)). | A repository whose submission resolves. | 1. `cogworks check --benchmark <id>`<br>2. `echo $?` | Exit 0, and the last line invites a `cogworks run`. | not run |
| CHECK-02 | P1 | none | A repository nothing can be found in exits 2, not 1 ([the exit code section](../terminal/check.md)). | An empty directory that is a git worktree with an origin remote. | 1. `cogworks check --benchmark <id>`<br>2. `echo $?` | Exit 2, and a report that names what was searched rather than a bare failure. | not run |
| CHECK-03 | P1 | none | The report never prints a bare list of booleans ([the report ordering](../terminal/check.md)). | Any repository. | 1. `cogworks check --benchmark <id>` | No line is only `True` or `False`. Every claim is a labelled value or a sentence. | not run |
| CHECK-04 | P1 | none | "Could not look" is said before anything it qualifies ([the survey section](../terminal/check.md)). | A repository with a module importing a package this machine lacks. | 1. `cogworks check --benchmark <id>` | The `could not finish` or `could not read` line appears above the read/skipped lines it qualifies. | not run |
| CHECK-05 | P1 | none | A skipped module is named with a reason, never folded into a count, unless it is the team's own runner script ([the survey section](../terminal/check.md)). | A repository with both a failing pipeline module and a `run_demo.py` that opens a missing file. | 1. `cogworks check --benchmark <id>` | The pipeline module is named with its reason. The demo script is counted, not named. | not run |
| CHECK-06 | P1 | none | The gap note is suppressed when the verdict already covers it ([the gap note section](../terminal/check.md)). | A machine missing graded packages, with a repository whose modules import them. | 1. `cogworks check --benchmark <id>` | Exactly one paragraph about missing packages, not two. | not run |
| CHECK-07 | P2 | tty | The progress spinner draws only to a terminal ([While it works](../terminal/check.md)). | A repository whose search takes more than a second. | 1. Run it in a terminal and watch.<br>2. Run it again with `2> log.txt` and read the file. | A live line with a braille spinner and a pairing count in the terminal. No escape codes in the file. | not run |
| CHECK-08 | P2 | tty | The estimate says "at most" and never appears before 200 attempts ([While it works](../terminal/check.md)). | A repository whose search runs thousands of pairings. | 1. Watch the live line from the first frame. | No estimate on the early frames. When one appears it ends `left at most`. | not run |
| CHECK-09 | P2 | tty | The live line is erased when the phase ends ([While it works](../terminal/check.md)). | Same. | 1. Watch until the search finishes. | The spinner line is gone from the final screen; what remains is a record, not the last animation frame. | not run |
| CHECK-10 | P1 | none | The four "nothing was searched for" sentences are distinct and each sends the reader somewhere different ([the no-submission branches](../terminal/check.md)). | Four repositories: benchmark not installed; an installed entry point; a root `submission.py`; a benchmark that does not describe its task. | 1. Run each. | Four different sentences. Record all four verbatim. | not run |
| CHECK-11 | P2 | none | `--json` prints parseable JSON and no progress ([Modifiers](../terminal/check.md)). | Any repository. | 1. `cogworks check --benchmark <id> --json \| python -m json.tool` | Valid JSON. `localGap` is present whether or not discovery ran. | not run |
| CHECK-12 | P2 | device | `--update-setup` sends four step names and prints them ([Modifiers](../terminal/check.md)). | A linked device, in the team's repository, with a check that exits 0. | 1. `cogworks check --benchmark <id> --update-setup` | A line beginning `setup: updated ` naming clone, environment, project, wiring. The setup page reflects it within its poll interval. | not run |
| CHECK-13 | P2 | device | `--update-setup` after a failing check does not send anything ([Modifiers](../terminal/check.md)). | A repository that exits 2. | 1. `cogworks check --benchmark <id> --update-setup` | No `setup:` line. Exit 2. The setup page is unchanged. | not run |
| CHECK-14 | P2 | none | The deprecated alias warns on stderr and still runs ([Edge cases](../terminal/check.md)). | Any repository. | 1. `cogworks doctor --benchmark <id> 2> err.txt` | `err.txt` begins `cogworks: \`doctor\` is deprecated; use \`cogworks check\`.` and the report still prints on stdout. | not run |
| CHECK-15 | P3 | none | The hosted interpreter line appears only when it differs from the local one ([Asking](../terminal/check.md)). | Python 3.11 locally, benchmark `language-search`. Then repeat with a 3.8 interpreter if available. | 1. Run both. | The `hosted python` line appears in the first and is absent in the second. | not run |

Not checkable by hand:

- Whether the search's pairing total is genuinely counted rather than estimated. That is settled by the tests, not by watching.

## terminal/report.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REPORT-01 | P1 | none | With no path it reads the most recently modified report ([Asking](../terminal/report.md)). | Two saved reports; `touch` the older one. | 1. `cogworks report` | The touched report is printed, not the one made most recently. | not run |
| REPORT-02 | P1 | none | With no reports it refuses with a sentence naming the next command ([Answered without work](../terminal/report.md)). | An empty `.cogbench/reports/`. | 1. `cogworks report`<br>2. `echo $?` | `cogworks: No local reports found. Run \`cogworks run\` first.` Exit 2. | not run |
| REPORT-03 | P1 | none | The first line marks the result self-reported ([The simple case](../terminal/report.md)). | Any saved report. | 1. `cogworks report` | The first line ends `· LOCAL · SELF-REPORTED`. | not run |
| REPORT-04 | P2 | none | A dirty commit is marked ([The simple case](../terminal/report.md)). | A report made from a dirty tree. | 1. `cogworks report` | The commit line ends ` (dirty)`. | not run |
| REPORT-05 | P2 | none | Nothing is written ([Answered without work](../terminal/report.md)). | Any report. | 1. Record the reports directory's bytes and mtimes.<br>2. `cogworks report`<br>3. Compare. | Unchanged. | not run |

## terminal/run.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| RUN-01 | P1 | none | The report is written under the directory the command started in ([How it ends](../terminal/run.md)). | Week 1, which chdirs into a scratch directory mid-run. | 1. `cogworks run --benchmark audio-identification`<br>2. `cogworks report` | The `saved:` path is under the starting directory's `.cogbench/reports/`, and `cogworks report` finds it. | not run |
| RUN-02 | P1 | none | A repository with nothing scoreable names the command that explains why ([Answered without work](../terminal/run.md)). | An empty repository. | 1. `cogworks run --benchmark <id>` | `cogworks: Nothing in this repository could be scored yet. Run \`cogworks check --benchmark <id>\` to see what was found.` Exit 2. | not run |
| RUN-03 | P1 | device, discord | `--live` opens one bubble and says which delivery happened ([Asking](../terminal/run.md)). | Linked device, committed repository, bound channel. | 1. `cogworks run --benchmark <id> --live 2> err.txt` | `err.txt` contains `live: one progress bubble opened in your team channel`. One message appears in the channel. | not run |
| RUN-04 | P1 | device | `--live` on an unbound channel says so and names the command that fixes it ([Asking](../terminal/run.md)). | Linked device, committed repository, no bound channel. | 1. `cogworks run --benchmark <id> --live 2> err.txt` | `live: synced to CogPortal; a team maintainer can choose the Discord channel with /cog` | not run |
| RUN-05 | P1 | device | `--live` refuses an uncommitted repository ([Asking](../terminal/run.md)). | A repository with no commits. | 1. `cogworks run --benchmark <id> --live` | `cogworks: Live sharing requires a committed GitHub repository.` Exit 2. | not run |
| RUN-06 | P1 | device, discord | Ctrl+C mid-run reports the run as failed to the team ([Cancel and interrupt](../terminal/run.md)). | A `--live` run in its evaluation phase. | 1. Press Ctrl+C.<br>2. Read the channel bubble and the exit code. | stderr `cogworks: interrupted`, exit 130, and the bubble reaches a stopped state rather than staying on a live loader. Record how long the bubble takes to settle. | not run |
| RUN-07 | P2 | device, offline | Live updates pause with one warning, not one per event ([While it works](../terminal/run.md)). | A `--live` run; pull the network mid-run. | 1. Watch stderr for the rest of the run. | Exactly one `cogworks: live updates paused: ` line, not a repeat per event. The run itself completes and the report is saved. | not run |
| RUN-08 | P2 | device | The replay batch carries the history at the end ([How it ends](../terminal/run.md)). | A `--live` run that completes. | 1. Watch the run surface page during and after. | The surface's event list is complete after the run finishes even if intermediate events were dropped. | not run |
| RUN-09 | P2 | none | The hidden `test` subcommand runs one fast case ([Modifiers](../terminal/run.md)). | Any working repository. | 1. `cogworks test --benchmark <id>`<br>2. Time it against `cogworks run`. | Completes markedly faster and still saves a report. Record both durations. | not run |
| RUN-10 | P3 | none | The hash seed is pinned before the first repository read ([Edge cases](../terminal/run.md)). | A repository whose result depends on set iteration order. | 1. Run three times.<br>2. Compare the primary metric. | Identical across the three runs. | not run |

## terminal/link.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| LINK-01 | P1 | tty | The privacy sentence prints before the handshake ([Asking](../terminal/link.md)). | An unlinked device. | 1. `cogworks link --portal <origin> --no-browser` | Before the code appears: `CogPortal receives setup check names, package versions, and your GitHub repository; never source, paths, logs, predictions, scores, or environment variables.` | not run |
| LINK-02 | P1 | tty | The code and the URL are printed and the browser opens unless suppressed ([Asking](../terminal/link.md)). | Unlinked device. | 1. `cogworks link --portal <origin>` | A line `Open {url} and confirm code {XXXX-XXXX-XXXX}.` and a browser tab opens. With `--no-browser`, no tab. | not run |
| LINK-03 | P1 | device, fresh account | Approving before joining a team kills the code silently (suspected bug) ([Cancel and interrupt](../terminal/link.md)). | An account that has signed in but has no team. | 1. `cogworks link --portal <origin>`<br>2. Open the URL and try to approve.<br>3. Watch the terminal. | Record what the browser does and what the terminal says. The document expects the browser to bounce to `/join` or `/connect` and the terminal to poll for the full ten minutes before saying the link expired. | not run |
| LINK-04 | P1 | none | A poll interrupted by one transient 5xx ends the link (suspected bug) ([Cancel and interrupt](../terminal/link.md)). | A proxy that answers `/v1/cli/device/token` with one 502. | 1. `cogworks link --portal <origin>` | Record whether the command retries or fails. The document expects it not to retry. | not run |
| LINK-05 | P1 | none | On success the token is saved with tight permissions ([How it ends](../terminal/link.md)). | Approve normally. | 1. Complete a link.<br>2. `ls -l ~/.cogbench ~/.cogbench/config.json` | The directory is `0700`, the file `0600`. stdout has `Linked {host}. Local commands still work offline.` | not run |
| LINK-06 | P2 | none | Linking outside a worktree still exits 0 and says what to do next ([How it ends](../terminal/link.md)). | Run from `/tmp`. | 1. Complete a link from a non-repository directory.<br>2. `echo $?` | Exit 0, and a stderr note telling the student to change into the team project. Record whether it names a real benchmark or the literal `<benchmark>`. | not run |
| LINK-07 | P2 | none | A failed clone-step update does not fail the link ([How it ends](../terminal/link.md)). | Link from a worktree whose repository the portal does not recognise. | 1. Complete a link.<br>2. `echo $?` | Exit 0, with `setup: clone was not updated: ` on stderr. | not run |
| LINK-08 | P2 | none | Letting the code expire says so plainly ([Cancel and interrupt](../terminal/link.md)). | Start a link and do nothing. | 1. Wait past ten minutes. | `cogworks: The device link expired before it was approved.` Exit 2. Record the elapsed time to confirm the ten-minute window. | not run |
| LINK-09 | P2 | none | The browser half labels the panel with the CLI's own name ([the browser half](../terminal/link.md)). | The connections page during a link. | 1. Read the panel label. | Record it. The document notes it reads `COGBENCH DEVICES` while every other string says CogWorks CLI. | not run |
| LINK-10 | P1 | none | Leaving the page within 900 ms of approval does not navigate after unmount (suspected bug) ([the browser half](../terminal/link.md)). | A link started with `?return_to=setup`. | 1. Press "Approve device".<br>2. Within 900 ms, click away to another route. | Record whether the app then navigates to `/setup` from under you. | not run |

## terminal/sync.md

| ID | P | Needs | Claim | Setup | Steps | Expected | Result |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SYNC-01 | P1 | device | A report syncs and prints one line ([The simple case](../terminal/sync.md#the-simple-case)). | A saved report with no weights, and a linked device. | 1. `cogworks sync`<br>2. `echo $?` | `Synced local_<hex> as LOCAL · SELF-REPORTED.` Exit 0. It appears on the dashboard. | not run |
| SYNC-02 | P1 | device | An untracked weight file fails the whole command after the report is already synced (suspected bug) ([How it ends](../terminal/sync.md#how-it-ends)). | A report recording a weight path that is not committed and does exist. | 1. `cogworks sync`<br>2. `echo $?`<br>3. Check the dashboard. | Record everything. The document expects the success line, then `cogworks: Failed to sync weight file ...: API route not found.`, exit 2, and the report present on the dashboard anyway. | not run |
| SYNC-03 | P1 | device | A committed weight file prints a note and uploads nothing ([How it ends](../terminal/sync.md#how-it-ends)). | A report recording a committed weight path. | 1. `cogworks sync` | `weights: {path} (committed file travels with repo)` and exit 0. | not run |
| SYNC-04 | P1 | device | The request URL carries an absolute local path (suspected bug, contradicts the link privacy promise) ([Edge cases](../terminal/sync.md#edge-cases)). | Same as SYNC-02, with a proxy or the dev worker logging request paths. | 1. `cogworks sync`<br>2. Read the logged request path. | Record it. The document expects a percent-encoded absolute path including the home directory. | not run |
| SYNC-05 | P1 | device | `weightsUsed` does not survive the upload (suspected bug) ([How it ends](../terminal/sync.md#how-it-ends)). | A report whose JSON contains `weightsUsed`. | 1. `cogworks sync`<br>2. `GET /api/v1/local-reports?benchmark=<id>` and read the row. | Record whether `weightsUsed` is present on the stored report. | not run |
| SYNC-06 | P2 | device | A recorded weight file that no longer exists is skipped in silence (suspected bug) ([Edge cases](../terminal/sync.md#edge-cases)). | A report recording a path, then delete that file. | 1. `cogworks sync` | Record whether any line mentions it. The document expects none. | not run |
| SYNC-07 | P2 | device | `outputDigest` is stripped before sending ([The simple case](../terminal/sync.md#the-simple-case)). | Any report. | 1. Capture the request body with a proxy.<br>2. Compare with the file on disk. | The file has `outputDigest`; the body does not. | not run |
| SYNC-08 | P2 | device | Syncing the same report twice from the same account updates rather than duplicates ([Edge cases](../terminal/sync.md#edge-cases)). | A synced report. | 1. `cogworks sync` again.<br>2. Count rows on the dashboard. | One row, not two. Exit 0 both times. | not run |
| SYNC-09 | P2 | device, second account | Syncing another account's report id is refused ([Asking](../terminal/sync.md#asking)). | A report already synced by another account, copied to this machine. | 1. `cogworks sync <path>` | `cogworks: That report ID belongs to another account.` Exit 2. | not run |
| SYNC-10 | P3 | device | With no path, the most recently modified report wins ([Edge cases](../terminal/sync.md#edge-cases)). | Two reports; `touch` the older. | 1. `cogworks sync` | The touched report's id is the one printed. | not run |
| SYNC-11 | P2 | device | Nothing is written locally ([Cancel and interrupt](../terminal/sync.md#cancel-and-interrupt)). | Any report. | 1. Record the reports directory's bytes and mtimes.<br>2. `cogworks sync`<br>3. Compare. | Unchanged, including after a weight failure. | not run |

Not checkable by hand:

- Whether a hosted run can read an uploaded weight file. There is no fetch path in the sandbox, so there is nothing to observe until one exists.
- Whether a failed weight upload should be fatal. That is a product call, stated in the document's open questions.
- The behaviour of `git ls-files` timing out. Producing a five-second stall in git is not reasonable by hand; the code path is stated in the document instead.
